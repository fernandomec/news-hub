<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Esqueceu a Senha</title>
    <link rel="shortcut icon" href="/imgs/SHORTCUTT.png">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/login_register.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"/>
</head>
<body>
    <a href="/login" class="back-button">Voltar para Login</a>

    <div class="login-container">
        <h1>Recuperar Senha</h1>
        <p class="subtitle">Digite seu e-mail para enviarmos um link de redefinição de senha.</p>

        <form id="forgotPasswordForm" action="/forgot-password" method="POST">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" required>
            </div>
            
            <button type="submit">Enviar link de redefinição</button>
        </form>

        <p class="register-link" style="margin-top: 20px;">Lembrou sua senha? <a href="/login">Faça Login</a></p>

        <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
            <div class="message error">
                <% errors.forEach(function(errorMsg) { %>
                    <p><%= errorMsg %></p>
                <% }); %>
            </div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="message success">
                <p><%= success %></p>
            </div>
        <% } %>
    </div>
</body>
</html>