<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
    <link rel="shortcut icon" href="/imgs/SHORTCUTT.png">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/login_register.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"/>
</head>
<body>
    <a href="/" class="back-button">Voltar</a>

    <div class="login-container">
        <h1>Criar Conta</h1>
        <p class="subtitle">Use seu e-mail para se registrar</p>

        <form id="registerForm" action="/register" method="POST">
            <div class="form-group">
                <label for="username">Nome de Usuário</label>
                <input type="text" id="username" name="username" value="<%= typeof username !== 'undefined' ? username : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="password">Senha</label>
                <div class="password-input-container">
                    <input type="password" id="password" name="password" required>
                    <span class="material-symbols-outlined toggle-password-icon" 
                          data-target="password" 
                          title="Mostrar senha" 
                          style="user-select: none;">visibility</span>
                </div>
                <small class="password-requirements">
                    <div class="requirement invalid" data-req-type="length">A senha deve conter: 8-20 caracteres</div>
                    <div class="requirement invalid" data-req-type="number">• Pelo menos 1 número</div>
                    <div class="requirement invalid" data-req-type="special">• Pelo menos 1 caractere especial (!, @, #, $, %, ^, &, *)</div>
                </small>
            </div>
            
            <button type="submit">Registrar</button>
        </form>

        <p class="register-link">Já possui conta? <a href="/login">Faça login</a></p>
        
        <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
            <div id="errorMessage" class="message error">
                <% errors.forEach(function(errorMsg) { %>
                    <p><%= errorMsg %></p>
                <% }); %>
            </div>
        <% } else { %>
            <div id="errorMessage" class="message error hidden"></div>
        <% } %>
        
        <% if (typeof success !== 'undefined' && success) { %>
            <div id="successMessage" class="message success">
                <p><%= success %></p>
            </div>
        <% } else { %>
            <div id="successMessage" class="message success hidden"></div>
        <% } %>
    </div>
    
    <script src="/js/password-toggle.js"></script><!--js para exibir emoji de um olho abrindo e fechando para ocultar ou deixar visivel senha ao digitar-->
    <script src="/js/senhaRegistro.js"></script><!--js para verificar se a senha atende aos requisitos-->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            initializePasswordValidation('password', '.password-requirements');
        });
    </script>
</body>
</html>